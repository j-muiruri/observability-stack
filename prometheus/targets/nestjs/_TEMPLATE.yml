# Copy to <service-name>.yml. App uses @willsoto/nestjs-prometheus,
# default /metrics endpoint.
- targets:
    - "SERVICE_HOST:3000"
  labels:
    job: "nestjs"
    service: "SERVICE_NAME"
    team: "TEAM_NAME"
    env: "prod"
    tier: "2"
    stack: "nestjs"
