# Copy to <service-name>.yml. App exposes /metrics via prom-client's
# collectDefaultMetrics() + a Registry.
- targets:
    - "SERVICE_HOST:3000"
  labels:
    job: "nodejs"
    service: "SERVICE_NAME"
    team: "TEAM_NAME"
    env: "prod"
    tier: "3"
    stack: "nodejs"
